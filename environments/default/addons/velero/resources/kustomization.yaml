---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Addon-owned resources for velero, synced by the addon's Application -> they exist iff the addon
# is enabled on the cluster.
resources:
  - namespace.yaml
