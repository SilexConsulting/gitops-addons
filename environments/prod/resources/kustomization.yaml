apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Environment-scoped resources for env=prod. Add manifests here that should apply to every
# prod cluster (and list them under `resources:`). Empty is valid — it renders nothing.
resources: []
