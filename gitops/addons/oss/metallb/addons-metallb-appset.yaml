# nonk8s
---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: addons-metallb
  namespace: argocd
spec:
  syncPolicy:
    # GIT-3: never cascade-delete. The CRDs hold every cluster's pools/peers; losing them drops
    # every LoadBalancer IP.
    preserveResourcesOnDeletion: true
  generators:
    - merge:
        mergeKeys: [server]
        generators:
          - clusters:
              values:
                addonChart: metallb
                addonChartVersion: 0.16.1
                addonChartRepository: https://metallb.github.io/metallb
              selector:
                matchExpressions:
                  - key: enable_metallb
                    operator: In
                    values: ['true']
          - clusters:
              selector:
                matchLabels:
                  environment: dev
              values:
  template:
    metadata:
      name: addon-{{name}}-{{values.addonChart}}
    spec:
      project: addons
      sources:
        - repoURL: '{{metadata.annotations.addons_repo_url}}'
          targetRevision: '{{metadata.annotations.addons_repo_revision}}'
          ref: values
          # --- Addon-owned resources (exist iff this addon is enabled) --------------------------
          # MetalLB does nothing until it is given address pools + advertisements (CRs). Those are
          # site-specific, so the catalogue ships none. Two ways to provide them:
          #   1. Private repo (recommended, per cluster): put the CRs in
          #      <private>/addons/clusters/<cluster>/metallb/resources/ and target this appset with
          #      gitops-private's `add-private-source-with-resources.yaml` patch.
          #   2. Catalogue-wide default (only if every cluster shares the same pools): uncomment
          #      the path below and list the CRs in environments/default/addons/metallb/resources/
          #      (see the commented examples there).
          # path: environments/default/addons/{{values.addonChart}}/resources
        - chart: '{{values.addonChart}}'
          repoURL: '{{values.addonChartRepository}}'
          targetRevision: '{{values.addonChartVersion}}'
          helm:
            releaseName: '{{values.addonChart}}'
            ignoreMissingValueFiles: true
            valueFiles:
              - $values/environments/default/addons/{{values.addonChart}}/values.yaml
              - $values/environments/{{metadata.labels.environment}}/addons/{{values.addonChart}}/values.yaml
              - $values/clusters/{{name}}/addons/{{values.addonChart}}/values.yaml
      destination:
        namespace: metallb-system
        name: '{{name}}'
      syncPolicy:
        automated: {}
        # Config CRs (pools/peers) can race the chart's CRDs + validating webhook on first sync.
        retry:
          limit: 10
          backoff:
            duration: 10s
            factor: 2
            maxDuration: 3m
        # The namespace is created by Argo (CreateNamespace), so it exists iff the addon is enabled.
        # The speaker needs hostNetwork + NET_RAW: label it privileged in case the cluster enforces
        # a stricter Pod Security default.
        managedNamespaceMetadata:
          labels:
            pod-security.kubernetes.io/enforce: privileged
            pod-security.kubernetes.io/audit: privileged
            pod-security.kubernetes.io/warn: privileged
        syncOptions:
          - CreateNamespace=true
          - ServerSideApply=true # Big CRDs.
